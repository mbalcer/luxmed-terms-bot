luxmed:
  email: ${LUXMED_EMAIL:}
  password: ${LUXMED_PASSWORD:}
notification:
  telegram:
    chat-id: ${TELEGRAM_CHAT_ID:}
    token: ${TELEGRAM_TOKEN:}

logging:
  level:
    pl.mbalcer.luxmedreservation: debug

spring:
  application.name: luxmed-search-terms
  datasource:
    url: jdbc:postgresql://luxmed-terms-db:5432/luxmed
    username: admin
    password: admin123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true